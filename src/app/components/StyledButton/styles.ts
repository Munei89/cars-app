import styles from 'styled-components/macro';
import Button from '@mui/material/Button';

export const StyledBtn = styles(Button)`

  $.MuiButton-root {
    background-color: #000;
    color: #fff;
  }
`;
